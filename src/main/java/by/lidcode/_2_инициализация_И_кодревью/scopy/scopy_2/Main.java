package by.lidcode._2_инициализация_И_кодревью.scopy.scopy_2;

public class Main {
    public static void main(String[] args) {
    /*    ApplicationContext context = new AnnotationConfigApplicationContext(AppConfig.class);

        // Получение синглтон-бина дважды
        MySingletonBean singleton1 = context.getBean(MySingletonBean.class);
        MySingletonBean singleton2 = context.getBean(MySingletonBean.class);

        // Получение прототип-бина дважды
        MyPrototypeBean prototype1 = context.getBean(MyPrototypeBean.class);
        MyPrototypeBean prototype2 = context.getBean(MyPrototypeBean.class);

        // Проверим, одинаковые ли ссылки
        System.out.println("Singleton бины равны? " + (singleton1 == singleton2)); // true
        System.out.println("Prototype бины равны? " + (prototype1 == prototype2)); // false

     */
    }
}
